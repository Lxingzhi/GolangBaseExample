// ptrTest project main.go
package main

import (
	"fmt"
)

const MAX int = 3

func main() {
	/*	fmt.Println("Hello World!")
		a := []int{1, 2, 2}
		var i int
		var ptr [MAX]*int
		for i = 0; i < MAX; i++ {
			ptr[i] = &a[i]
			//		fmt.Printf("ptr[&d] = %dn", i, ptr[i])
		}
		for i = 0; i < MAX; i++ {
			fmt.Printf("a[%d]=%dn", i, *ptr[i])
			//		fmt.Printf("ptr[&d] = %dn", i, ptr[i])
		}
	*/
	/*
		var a int
		var ptr *int
		var pptr **int

		a = 3000


		// 指针 ptr 地址
		ptr = &a

		// 指向指针 ptr 地址
		pptr = &ptr

		// 获取 pptr 的值
		fmt.Printf("变量 a = %dn", a)
		fmt.Printf("指针变量 *ptr = %dn", ptr)
		fmt.Printf("指向指针的指针变量 **pptr = %dn", pptr)
	*/

}
